name: Trigger auto deployment for ragpgpy22-mq5rtpyml-ca

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/ragpgpy22-mq5rtpyml-ca-AutoDeployTrigger-565cfea1-3928-4085-ae76-2b9ef89a591e.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.RAGPGPY22MQ5RTPYMLCA_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.RAGPGPY22MQ5RTPYMLCA_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.RAGPGPY22MQ5RTPYMLCA_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: ragpgpy22mq5rtpymlxsggregistry.azurecr.io
          registryUsername: ${{ secrets.RAGPGPY22MQ5RTPYMLCA_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.RAGPGPY22MQ5RTPYMLCA_REGISTRY_PASSWORD }}
          containerAppName: ragpgpy22-mq5rtpyml-ca
          resourceGroup: ResourceGroup1
          imageToBuild: ragpgpy22mq5rtpymlxsggregistry.azurecr.io/ragpgpy22-mq5rtpyml-ca:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


